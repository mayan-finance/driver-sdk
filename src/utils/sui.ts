import { SuiClient } from '@mysten/sui/client';

export async function readSuiTable(suiClient: SuiClient, tableId: string) {
	// const dynamicFields = await suiClient.getDynamicField({parentId: tableId});
}
